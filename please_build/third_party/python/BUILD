package(default_visibility = ["PUBLIC"])

pip_library(
    name = "diffusers",
    version = "0.16.1",
    zip_safe = False, # has shared object files which can't be linked to them when zipped up
)

pip_library(
    name = "torch",
    version = "2.0.0",
    zip_safe = False, # has shared object files which can't be linked to them when zipped up
)

pip_library(
    name = "transformers",
    version = "4.28.1",
    zip_safe = False, # has shared object files which can't be linked to them when zipped up
)

pip_library(
    name = "datasets",
    version = "2.12.0",
    zip_safe = False, # has shared object files which can't be linked to them when zipped up
)

pip_library(
    name = "fire",
    version = "0.5.0",
    zip_safe = False, # has shared object files which can't be linked to them when zipped up
)

pip_library(
    name = "numpy",
    version = "1.24.3",
    zip_safe = False, # has shared object files which can't be linked to them when zipped up
)

pip_library(
    name = "huggingface_hub",
    version = "0.14.1",
    zip_safe = False, # has shared object files which can't be linked to them when zipped up
)

pip_library(
    name = "requests",
    version = "2.30.0",
    zip_safe = False, # has shared object files which can't be linked to them when zipped up
)

pip_library(
    name = "urllib3",
    version = "2.0.2",
    zip_safe = False, # has shared object files which can't be linked to them when zipped up
)

pip_library(
    name = "charset-normalizer",
    version = "3.1.0",
    zip_safe = False, # has shared object files which can't be linked to them when zipped up
)

 

 